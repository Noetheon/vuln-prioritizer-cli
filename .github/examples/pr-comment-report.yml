name: Example Consumer Workflow - PR Comment

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  prioritize:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run vuln-prioritizer action
        id: prioritize
        uses: Noetheon/vuln-prioritizer-cli@v1.1.0
        with:
          mode: analyze
          input: trivy-results.json
          input-format: trivy-json
          output-format: markdown
          output-path: vuln-prioritization.md
          asset-context: assets.csv
          vex-files: |
            openvex.json
          policy-profile: enterprise

      - name: Publish PR comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require("fs");
            const body = fs.readFileSync("${{ steps.prioritize.outputs.report-path }}", "utf8");
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });
